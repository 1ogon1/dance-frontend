import React, { useState } from "react"
import "./Table.css"
export const Table = () => {
    const [role, setRole] = useState("admin")

    return (
        <div style={{width: "100%", height: "100vh"}}>
            Table
        </div>

    )
}